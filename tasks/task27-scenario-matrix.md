# Task 27：Spec §29 系统 E2E 场景矩阵

## 验收口径

`SYSTEM-GREEN` 必须在同一场景中跨过真实 Chromium、Authority、Gateway 和 Artifact Store，并断言最终业务/安全/发布结果。纯函数、Schema、fake Page、`playwright --list` 或单包集成测试只能记为 `SUPPORTING-ONLY`。

| # | 场景 | 状态 | 系统 E2E 证据 / 待补缺口 |
|---:|---|---|---|
| 1 | 完整只读主链 accepted | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts`：Engine 从真实 PRD manifest/diff/scope 与 DecisionReceipt 密封 readiness，Compiler Input 绑定已审批 runId；真实 Chrome、三项细粒度 capability、fresh Run Authority 状态复验、固定 Chrome/Gateway、执行瞬间只读快照与 token 级源码扫描全部通过后，生成 Playwright 只能经同 session/launcher 的 Controlled Read Bridge 触发唯一一次浏览器 Runner。Browser Results 与 3 份脱敏证据直接取自该次执行；staging 精确对账 execution 的 approval、Case 集合、exitCode、Chrome/Proxy 测量和报告摘要后才允许 27 类同代资产闭环。 |
| 2 | PRD 歧义暂停并从原节点恢复 | SYSTEM-GREEN | `scripts/e2e-system-flow.golden.test.ts` 将角色歧义冻结为 pending ticket，Authority proof 同时绑定原节点、原事件链和用户决定摘要；Scope DecisionReceipt 进一步签名绑定问题、decisionId 与“仅使用 auditor”解析文本。恢复只能回到冻结节点，随后经真实 Chrome、签名 Gateway、Authority Read Grant 执行，Artifact Store staging 重读 workflow proof、Scope receipt、Gateway audit 和结果后才发布 accepted |
| 3 | 附件变化触发 Revision 和范围重审 | SYSTEM-GREEN | 同一 Golden 以附件原始 bytes 变化生成新 PRD Revision，影响分析只标记附件来源实体；新代显式生成 entityKind/semanticKey 快照并复算稳定 ID lineage，Scope 与 Lineage 均由专用 Authority DecisionReceipt 重批。旧 Revision Grant 对新主体复验为 `E2E_APPROVAL_SUBJECT_MISMATCH`，新代真实 Chrome/Gateway 执行后由 staging 从前后实体快照复算 mapping、验签两类决定并激活新代 |
| 4 | 页面 URL/身份错误不继续绑定 | SYSTEM-GREEN | `scripts/e2e-system-flow.golden.test.ts` 让真实 Chrome 经受控 Gateway 从 `/orders` 重定向到 `/login`；Discovery preflight 在 Oracle 前形成 `E2E_RUNTIME_PAGE_URL_MISMATCH/environment-blocked`，不会签发 Read Grant。Gateway 对两次请求逐条形成 Ed25519 审计，结果保留非空 gateway-audit 证据，staging 重读 reasonCode、证据和签名 counters 后发布 environment-blocked |
| 5 | 角色错误为 input-blocked | SYSTEM-GREEN | 真实 Chrome 页面声明 ordinary-user，而 Discovery subject 批准 auditor；preflight 形成 `E2E_RUNTIME_ROLE_MISMATCH/input-blocked`，不记录业务失败也不能签执行 Grant。签名 Gateway 审计、非空证据、最终 incomplete verdict 均由 Artifact Store staging 独立重读复核 |
| 6 | 非生产可恢复写批准、验证和清理 | SYSTEM-GREEN | `scripts/e2e-write.golden.test.ts` 由受信 Projector/Compiler 生成密封 Playwright 项目，Discovery 对真实 Source Set 与本地 CLI 签名；Host trust token 固定 Authority、Chrome bytes 与 Gateway Proxy，fresh Run 动态复验撤销/过期状态，固定 launcher 在执行瞬间重验工具链、重建并递归密封私有快照。生成测试只能持有同 session/launcher 派生的 opaque Bridge handle，经一次性 RunGate、认证 Authority RPC、Gateway、Lease、不可变 Cleanup Plan 和独立 `ExecutionOutcomeReceipt` 驱动真实 Chrome，完成双 HTTP intent、verified-clean、Lease release、Attempt/Gateway reservation 与 27 资产 accepted；Playwright JSON reporter 的逐 Case execution fact 与 Browser Results/报告摘要由 staging 对账。该能力标注为 `TRUSTED-COMPILER + SOURCE-ATTESTED + GATEWAY-CONTROLLED-WRITE`，明确不宣称 `PRODUCTION-ISOLATED`。 |
| 7 | 旧审批因 target/payload/environment/Revision 变化失效 | SYSTEM-GREEN | `ReversibleWriteGateway` 现强制接收执行时独立 currentSubject，并通过本地或认证 RPC 调用 Authority `verifyForSubject`，不再只验旧 Grant 或把 `grant.subject` 冒充当前计划。Golden 分别改变 Revision、environment、target fingerprint 和 payload digest；Runtime 与 Gateway 两道门均在浏览器动作/HTTP 转发前阻断，签名审计精确记录 blocked=4、forwarded=0，staging 再验签并发布 safety-blocked |
| 8 | 生产不可逆操作永久拒绝 | SYSTEM-GREEN | Authority 对 production 与 irreversible-write 均拒绝签发 capability，真实 Chrome 保持 about:blank、业务导航未开始；独立 Gateway 对零请求形成可验签的 forwarded=0 审计，Artifact Store staging 重读拒绝码、accepted=false 与签名 counters 后发布 safety-blocked |
| 9 | 直接运行生成 Playwright 仍被 runGate 阻断 | SYSTEM-GREEN | Compiler 生成的真实 Playwright 项目缺 Bridge/RunGate 时在浏览器动作前 fail-closed；正式路径只能由启动期固定 trust token、不可伪造 trusted session 与 opaque Bridge handle 进入固定 launcher，session、RunGate 和 action 均一次性消费。业务请求自选 Authority/Discovery key、Chrome、Proxy、Bridge endpoint、额外/人工源码、伪 Discovery/freshness 签名和 Source Set 篡改均在浏览器与副作用前被拒绝。 |
| 10 | 真实链路响应不被注入 | SYSTEM-GREEN | read-only 与 injection/healing Golden 真实基线 |
| 11 | HTTP 500 注入且 upstream mutation 为零 | SYSTEM-GREEN | `scripts/e2e-injection-healing.golden.test.ts` |
| 12 | Service Worker/WebSocket/Beacon 逃逸被阻断 | SYSTEM-GREEN | 同上 `CASE-PROTOCOL-ESCAPE` |
| 13 | 注入 matcher 未命中为 safety-blocked | SYSTEM-GREEN | `scripts/e2e-injection-healing.golden.test.ts` 使用 Authority 签名的精确 Injection Grant，真实 Chrome 故意发送 query 与 payload 均不匹配的请求；Gateway 返回 `E2E_GATEWAY_INJECTION_INTENT_NOT_FOUND`，审计为 `matched=0/blocked=1/injectionTargetForwarded=0`，真实上游 mutation 命中为零，独立 Injection Safety 产生 match-count/blocked reasonCodes，Artifact Store active generation 终态为 `safety-blocked` |
| 14 | Portal/虚拟列表 locator 安全自愈 | SYSTEM-GREEN | `scripts/e2e-injection-healing.golden.test.ts` 的真实 Chrome 页面同时包含 Portal 挂载按钮与真实窗口化列表：`订单 180` 初始不在 DOM，滚动到对应窗口后才渲染，且 DOM 始终最多保留 8 行；旧 role locator 对 Portal 按钮失败后形成 automation-blocked Attempt，语义与审批 subject 均未变化的 test-id 候选经正式 Healing Review 才进入下一 slot，最终注入请求仍由签名 Gateway 精确控制 |
| 15 | 写操作是否生效未知时不重试 | SYSTEM-GREEN | `scripts/e2e-write.golden.test.ts` 的断连场景让真实 Chromium 经 Authority Write Grant、Lease 和签名 Gateway 发出唯一 POST；上游确实完成一次 mutation 后代理主动切断响应，页面无法确认结果，Runtime 形成 `effectObservation=unknown`。Gateway 将 reservation 原子终结为 `unknown`；使用同一 capability 的第二次恶意尝试被 Authority/Gateway safety-blocked，forwarded=0、上游 mutationHits 仍为 1。资源随后被 Lease quarantine，Artifact Store staging 从落盘事实独立确认 unknown/禁重试后发布 `safety-blocked` active generation |
| 16 | 明确产品失败 rejected 且保留回归 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 让真实 Chrome 页面保持正确身份但返回与 PRD Oracle 不一致的业务文本，Runtime 形成带真实 screenshot/DOM/Gateway 证据和 Authority Attempt proof 的 `failed` 结果；同代经严格 Artifact Store 发布为 `rejected`，FinalReport 包含 `VERDICT_REQUIRED_OBLIGATION_FAILED`，且 active Regression Manifest 的 caseMappings、真实 `playwright --list` 结果和生成源码仍完整保留失败 Case |
| 17 | 必要 Case 部分执行全过仍 incomplete | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 的 blocked 场景执行一个必要 Case 且结果为 passed，同时第二个 active 必要 Case 以受信 blocked disposition 进入 Coverage Universe；同一真实 Chrome、Authority、Gateway、Artifact Store 闭环最终必须发布 `incomplete`，FinalReport 包含 `VERDICT_REQUIRED_CASE_MISSING`，执行覆盖率低于 100%，不会把“已执行部分全过”误判为 accepted |
| 18 | 零分母展示 not-applicable | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 从 Artifact Store active generation 重读 FinalReport，确认注入通过率与清理成功率均为 `status=not-applicable, 0/0`，再由同一真实 Chromium 打开该报告渲染结果并断言显示“不适用”且不显示伪造的 `0.00%` |
| 19 | 证据 canary 脱敏失败阻断发布 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 在真实 Chrome 已产生 screenshot/DOM、Authority capability 已消费且 Gateway 审计已签名后，让视觉 Sanitizer 能力 canary 返回 `detected=false`；Artifact Store 的 `publishPrepared` 准备阶段以 `E2E_PRIVACY_CANARY_FAILED` fail-closed，且 `readActive` 保持为空，失败证据不会生成伪造脱敏证明或切换 active generation |
| 20 | 报告 XSS/路径逃逸被拒绝 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 让恶意 `<img onerror>` 标题进入真实 Chrome、Authority、Gateway、Artifact Store 形成的 active FinalReport，再由真实 Chromium 渲染：页面只含字面文本、没有 `<img>` 节点且全局执行标记未出现；基于同代报告构造的 `../escape.png` evidence link 在渲染前由正式 RelativePath/FinalReport Schema 以 `E2E_REPORT_INPUT_INVALID` fail-closed |
| 21 | 发布崩溃后 active 仍完整同代 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 先用真实 Chrome、Authority、Gateway 产物发布完整 27 类 accepted generation，再复用该真实 run 文件启动第二代发布并在首个文件 fsync 后模拟进程崩溃；`recover` 后 active 的 generationId/generationDigest/verdict 未改变，重读 FinalReport 与 Generation Manifest 均和崩溃前逐字段一致；其余 durable 窗口由 kill-point 恢复矩阵继续提供支持证据 |
| 22 | 两个并发 run 不覆盖同一 Asset | SYSTEM-GREEN | 新增 `scripts/e2e-concurrent-publication.golden.test.ts`：两个独立真实 Chromium 进程分别完成 Authority Discovery/Read Grant、Gateway 受控请求和 passed 业务断言后，同时用两个 Artifact Store helper 发布同一已有 Asset。OS 非阻塞锁只允许一个代际进入事务，另一个稳定返回 `E2E_ARTIFACT_LOCKED`；失败 Run 在锁释放后重试，以更高 fencing token 发布。staging 从各自落盘 result 独立核对 runId/generationId/fencing，validation reference 保护前一代，最终证明两代文件内容不交叉、fencing 单调且 active 只指向完整代际。并修复 helper 握手 stdout/exit 竞态，首次根目录创建竞态改为显式 `E2E_ARTIFACT_ROOT_RACE` fail-closed |
| 23 | blocked Case 不生成 skip 假测试 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 的 blocked 场景在同一代执行真实 Chrome executable Case，经 Authority、Gateway 和 Artifact Store 发布完整 27 资产 `incomplete` 报告；受信 Compiler 只把 blocked Case/reasonCode 写入生成 Run Bundle，Discovery 扫描并拒绝 skip/fixme/fail/only/todo，真实本地 `playwright --list --reporter=json`、源码和 caseMappings 均不存在 blocked Case，Ed25519 attestation 与 Regression Manifest 精确绑定 blocked 集合；Engine 要求 executable mapping/list 与 blocked 对 active Case 互斥闭合，FinalReport 生成 blocked disposition，必要 obligation 缺结果不可 accepted |
| 24 | 新 Revision 保持可证明稳定 ID lineage | SYSTEM-GREEN | 新增 typed `lineageMappings` 契约与 `reconcileEntityLineage`：Core 只按 entityKind/semanticKey 精确匹配，preserved 必须保持相同 ID，split/merged 仅接受 authority-confirmed 显式映射，重复/偷换 ID/未知实体 fail-closed；`scripts/e2e-lineage.golden.test.ts` 连续执行两个 PRD Revision 的真实 Chrome、独立 Gateway 和 Revision-bound Authority Grant，第二版保留 `REQ-ORDER-LIST` 并新增 `RULE-ORDER-SORT`，每代 Lineage Receipt 覆盖完整映射，Artifact Store staging 从前代实体快照独立复算并验签后才激活第二代 |
| 25 | final-report verdict 可独立复算 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 的 accepted 与 blocked/incomplete 两条真实 Chrome 场景都从 Artifact Store active generation 重读 `test-cases/run-bundle/browser-results/gateway-audit/workflow-events/verdict-input/final-report`，用 Authority Attempt proof verifier 重建 persisted Attempt audit，再独立调用 Verdict Engine 复算 verdict/reasonCodes/metrics，与 FinalReport 和 `verdictInputDigest` 精确对账 |
| 26 | 报告/回归/证据 generationId 与 digest 一致 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 由真实 Chromium 事实构建 27 类 Artifact；staging auditor 重读实际 bytes，校验同代引用、digest、回归 discovery、证据 proof、FinalReport/VerdictInput 后才切 active |
| 27 | raw quarantine 不进 Git/发布且按策略销毁 | SYSTEM-GREEN | `scripts/e2e-read-only.golden.test.ts` 将真实 Chromium 产生的 screenshot/DOM 先写入 Git 工作区外、0700 权限的 AES-256-GCM Quarantine，断言磁盘仅含不泄漏明文的认证密文；内存原副本立即清零，Sanitizer 只能通过 RBAC 审计读取后生成发布证据，完整 generation 明确不含 `raw/` 或 quarantine 路径。发布成功后以 active generation digest 触发 publisher crypto-erasure，发布失败则由 privacy-admin 执行恢复销毁；两条路径都证明数据密钥不存在、Run 目录不存在且审计链完整 |
| 28 | 缺浏览器/Gateway/Sanitizer 能力时明确阻塞 | SYSTEM-GREEN | 新增 `scripts/e2e-missing-capabilities.golden.test.ts` 三组真实系统探针：必需 Firefox executable 缺失时明确 `E2E_BROWSER_EXECUTABLE_UNAVAILABLE/environment-blocked`；真实 Chromium 连接已关闭的受控 Gateway 时明确 `E2E_RUNTIME_GATEWAY_UNAVAILABLE/environment-blocked`；真实截图进入没有 visual adapter 的正式 Sanitizer 时明确 `E2E_VISUAL_ADAPTER_UNAVAILABLE/safety-blocked`。每组均保留 Authority 签名、Gateway audit digest，并由 Artifact Store staging 重读 reasonCode/accepted=false 后发布阻塞代际，禁止静默跳过或伪造 accepted |
| 29 | Manual/N/A 对 accepted 的影响符合冻结策略 | SYSTEM-GREEN | 新增专用 `coverage-disposition-decision-receipt/v1`：登记的 coverage-approver 必须签名绑定 obligationId、Requirement Model digest、Coverage Policy digest、policyCode 和 rationale；Builder 与 staging 从落盘 Coverage Universe 重建 subject 并验签，禁止伪造 N/A ID。`scripts/e2e-read-only.golden.test.ts` 在同一真实 Chrome、Authority、Gateway、Artifact Store 代际中组合 automated passed、Authority 双人签名且未过期的 ManualResult passed、已签名 N/A 三类必要 obligation；最终独立复算为 accepted，同时报告逐项保留 manual-required、manual passed 与 not-applicable disposition，不把 Manual/N/A 冒充自动覆盖 |
| 30 | 未纳入 Firefox/WebKit 不得宣称跨浏览器 | SYSTEM-GREEN | Browser Results 新增必填 `executedBrowserIds`；Engine 将 Project Policy 批准矩阵、Execution Contract 计划矩阵与实际执行集合三层闭合，越权执行或 required 未执行会阻断发布。FinalReport `cannotClaim` 由三层事实确定性生成并在 staging 独立复算；`scripts/e2e-read-only.golden.test.ts` 的真实 Chrome/Authority/Gateway/Store active 报告明确包含“FIREFOX、WEBKIT 未完整批准、计划并执行，不能宣称跨浏览器兼容性”，真实 Chromium 报告页面同步可见 |

## 当前计数

- SYSTEM-GREEN：30 / 30
- IN-REVIEW：0 / 30
- SUPPORTING-ONLY：0 / 30
- MISSING：0 / 30
